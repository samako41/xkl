<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ── Report Action ──────────────────────────────────────────────────── -->
    <record id="action_report_xkl_sale_quote" model="ir.actions.report">
        <field name="name">XKL Proposal</field>
        <field name="model">sale.order</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">xkl_sale_quote.report_xkl_sale_quote_document</field>
        <field name="report_file">xkl_sale_quote.report_xkl_sale_quote_document</field>
        <field name="print_report_name">'XKL_Proposal_%s' % (object.name)</field>
        <field name="binding_model_id" ref="sale.model_sale_order"/>
        <field name="binding_type">report</field>
        <field name="paperformat_id" ref="base.paperformat_us"/>
    </record>

</odoo>
